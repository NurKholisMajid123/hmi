<div id="sidebar" class="active">
  <div class="sidebar-wrapper active">
    <div class="sidebar-header">
      <div class="d-flex justify-content-between">
        <div class="logo">
          <a href="/dashboard"><img src="/dashboard-template/dist/assets/images/logo/logo.png" alt="Logo"></a>
        </div>
        <div class="toggler">
          <a href="#" class="sidebar-hide d-xl-none d-block"><i class="bi bi-x bi-middle"></i></a>
        </div>
      </div>
    </div>
    <div class="sidebar-menu">
      <ul class="menu">
        <li class="sidebar-title">Menu</li>

        <!-- Dashboard -->
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'dashboard' ? 'active' : '' %>">
          <a href="/dashboard" class='sidebar-link'>
            <i class="bi bi-grid-fill"></i>
            <span>Dashboard</span>
          </a>
        </li>

        <!-- Admin Only: Manajemen User -->
        <% if (user && user.roleLevel === 1) { %>
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'users' ? 'active' : '' %>">
          <a href="/users" class='sidebar-link'>
            <i class="bi bi-people-fill"></i>
            <span>Manajemen User</span>
          </a>
        </li>
        <% } %>

        <!-- Admin, Ketua Umum, Ketua Bidang: Manajemen Bidang -->
        <% if (user && (user.roleLevel === 1 || user.roleLevel === 2 || user.roleLevel === 5)) { %>
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'bidang' ? 'active' : '' %>">
          <a href="/bidang" class='sidebar-link'>
            <i class="bi bi-diagram-3-fill"></i>
            <span>Manajemen Bidang</span>
          </a>
        </li>
        <% } %>

        <!-- Sekretaris: Manajemen Anggota Sekretaris -->
        <% if (user && (user.roleLevel === 1 || user.roleLevel === 2 || user.roleLevel === 3)) { %>
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'sekretaris' ? 'active' : '' %>">
          <a href="/departemen/sekretaris/anggota" class='sidebar-link'>
            <i class="bi bi-journal-text"></i>
            <span>Anggota Sekretaris</span>
          </a>
        </li>
        <% } %>

        <!-- Bendahara: Manajemen Anggota Bendahara -->
        <% if (user && (user.roleLevel === 1 || user.roleLevel === 2 || user.roleLevel === 4)) { %>
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'bendahara' ? 'active' : '' %>">
          <a href="/departemen/bendahara/anggota" class='sidebar-link'>
            <i class="bi bi-cash-stack"></i>
            <span>Anggota Bendahara</span>
          </a>
        </li>
        <% } %>

        <!-- Program Kerja - Semua role bisa akses -->
        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'proker' ? 'active' : '' %>">
          <a href="/proker" class='sidebar-link'>
            <i class="bi bi-card-checklist"></i>
            <span>Program Kerja</span>
          </a>
        </li>

        <!-- Pengurus: Laporan -->
        <% if (user && user.roleLevel <= 3) { %>
        <li class="sidebar-item has-sub">
          <a href="#" class='sidebar-link'>
            <i class="bi bi-file-earmark-text-fill"></i>
            <span>Laporan</span>
          </a>
          <ul class="submenu">
            <li class="submenu-item">
              <a href="/laporan/keuangan">Laporan Keuangan</a>
            </li>
            <li class="submenu-item">
              <a href="/laporan/kegiatan">Laporan Kegiatan</a>
            </li>
          </ul>
        </li>
        <% } %>

        <li class="sidebar-item <%= typeof activeMenu !== 'undefined' && activeMenu === 'profile' ? 'active' : '' %>">
          <a href="/profile" class='sidebar-link'>
            <i class="bi bi-person-fill"></i>
            <span>Profil Saya</span>
          </a>
        </li>
        <!-- Logout -->
        <li class="sidebar-item">
          <a href="/logout" class='sidebar-link'>
            <i class="bi bi-box-arrow-right"></i>
            <span>Logout</span>
          </a>
        </li>
      </ul>
    </div>
    <button class="sidebar-toggler btn x"><i data-feather="x"></i></button>
  </div>
</div>